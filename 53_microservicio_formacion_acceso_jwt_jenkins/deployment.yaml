apiVersion: apps/v1
kind: Deployment
metadata:
  name: formacion-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: formacion
  template:
    metadata:
      labels:
        app: formacion
    spec:
      containers:
        - name: formacion-container
          image: imgformacion  # Asegúrate de que este sea el nombre de tu imagen Docker
          imagePullPolicy: Never
          ports:
            - containerPort: 9005
          